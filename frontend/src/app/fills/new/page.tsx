import { PlaceholderPage } from "@/components/placeholder-page";

export default function NewFillPage() {
  return <PlaceholderPage description="대타 작성 폼은 기존 fill 생성 API를 REST로 분리한 뒤 실제 submit 로직과 연결합니다." icon="edit_square" title="모집글 올리기" />;
}
