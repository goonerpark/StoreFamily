<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.storefamily.mapper.LoginMapper">

	<select id="getLogin" parameterType="String" resultType="Member">
		select * from member where id=#{id} and pwd=#{pwd}
	</select>

	<select id="getStoreByCeoBno" parameterType="int" resultType="Member">
		select
			store_id as table_name,
			store_code as code,
			store_name as bussiness,
			store_address as bussinessaddress,
			store_phone as bussinessnum
		from store
		where ceo_bno = #{ceoBno}
		order by created_at desc
		limit 1
	</select>
</mapper>
